// The Swift Programming Language
// https://docs.swift.org/swift-book

/// Aura Design System — token-based visual primitives for the Aura SDUI framework.
public struct AuraDS {
    /// Current version of the Aura Design System module.
    public static let version = "0.1.0"

    /// A placeholder greeting used to verify the module is wired correctly.
    public static func hello() -> String {
        "Hello, AuraDS!"
    }
}
